const pusheen = require("..");

console.log(pusheen)

describe("pusheen", () => {
  it("can add", () => {
    // const sum = pusheen.add(1, 2);
    // expect(sum).toBe(3);

    // const p1 =  pusheen['Point#constructor'](100, 100);
    // const p2 =  pusheen['Point#constructor'](100, 100);
    // // const p3 = pusheen['Point#add'] p1.add(p2);
    // console.log(p2)
  });
});
